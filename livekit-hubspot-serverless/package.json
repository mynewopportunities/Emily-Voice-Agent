{
  "name": "livekit-google-sheets-webhook",
  "version": "2.0.0",
  "description": "Serverless webhook handler for LiveKit to Google Sheets integration",
  "main": "api/webhooks/livekit.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "googleapis": "^144.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
